import Swal from 'sweetalert2'
import withReactContent from 'sweetalert2-react-content'

function SweetloginSTN(){

const addsweet = Swal.fire({
    icon: 'error',
    title: '帳號已在別處登入',
    showConfirmButton: false,
    timer: 5000
  })
  return(
  <>
  </>
  )
}
  export default SweetloginSTN